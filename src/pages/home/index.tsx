import * as React from 'react';

export default class App extends React.Component {
  onChange = () => { };

  render() {
      return (
          <div className="app">
              1111
          </div>
      );
  }
}
